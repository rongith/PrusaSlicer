name: Create flatpak single file bundle

on:
  workflow_dispatch:

jobs:
  bundle_flatpak:
    uses: Prusa-Development/PrusaSlicer-Actions/.github/workflows/bundle_flatpak.yml@legacy
    secrets: inherit
    with:
      actions_branch: legacy
